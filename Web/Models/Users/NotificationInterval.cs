namespace MediaCommMvc.Web.Models.Users
{
    public enum NotificationInterval
    {
        None = 0,
        Immediate = 1
        /*
         * Delayed Notifications will require a job
         * ,
         * Daily,
         * Weekly*/
    }
}